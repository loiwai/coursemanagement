@model IEnumerable<mscs.Models.CouresRegistration>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RegDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CourseNoNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Student)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RegDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CourseNoNavigation.CourseNo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Student.StudentId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CourseRegNo">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CourseRegNo">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CourseRegNo">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
